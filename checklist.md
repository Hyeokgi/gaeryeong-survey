# 개선 체크리스트

개령면 농지전수조사 현장조사 웹앱(`kim.html`, `moon.html`)의 개선 작업 목록.
두 파일은 데이터·조사원명·저장키만 다르고 로직이 동일하므로, 모든 수정은 **두 파일에 똑같이** 적용한다.

---

## 1차 · 버그 수정 묶음 (코드 완료 · 푸시 대기)

### 담당 지역 오표기 수정
- [x] `WHO`(조사원명), `AREA`(행정구역) 상수를 스크립트 상단에 추가
- [x] 엑셀 `전체`·마을별 시트 제목의 하드코딩된 `동부리` 제거
- [x] 엑셀 `요약` 시트 제목·부제의 하드코딩된 `동부리` 제거
- [x] 보고용 표의 `<title>`·`<h1>`·부제 하드코딩 제거
- [x] CSV 파일명 `동부리_조사기록_…` → `{조사원명}_조사기록_…`
- [x] 백업 JSON 파일명 `동부리_진행상황_…` → `{조사원명}_진행상황_…`
- [x] 검증: kim.html 보고서·엑셀에 `광천리·덕촌리·서부리·양천리` 표시 확인

### 보고용 표에 방문일 열 추가
- [x] 표 머리글에 `방문` 열 추가
- [x] 각 행에 `{day}일차` 셀 추가
- [x] 검증: 보고서 각 행에 `N일차` 셀이 채워지는 것 확인

### 팝업 차단 시 대체 경로
- [x] 보고서 HTML을 먼저 문자열로 만든 뒤 `window.open` 시도
- [x] 차단되면 blob 링크를 담은 시트를 띄워 사용자가 직접 누를 수 있게 함
- [x] `a.opt` 앵커용 스타일 추가 (밑줄·색상 상속)
- [x] 검증: 브라우저에서 `window.open`을 막고 대체 시트가 뜨는 것 확인

### 마무리
- [x] 두 파일 JS 문법 검사 통과
- [x] 두 파일에 동일하게 적용됐는지 대조 (검증 스크립트가 양쪽 모두 통과)
- [x] 실제 브라우저에서 앱 렌더링·콘솔 오류 없음 확인
- [x] 엑셀 생성 확인 (6개 시트 · 3.7MB · `동부리` 문자열 없음)
- [x] 커밋
- [ ] **푸시 (사용자 확인 대기)** — 현장에서 사용 중인 링크라 반영 시점을 사용자가 정한다

---

## 2차 · 사용 개선 묶음 (완료)

### 사진 저장 취소 시 매수 오집계 수정
- [x] `shoot()`가 `saveOrShowPhoto()`의 반환값을 확인하도록 수정
- [x] 취소하면 매수가 오르지 않고 파일명 번호도 소모되지 않는지 확인 (AbortError 재현해 검증)

### 마지막으로 보던 마을·일차 기억
- [x] `POS` 키에 위치 저장 (백업 JSON에는 넣지 않는다 — 진행상황과 성격이 다름)
- [x] 앱 시작 시 복원, 저장된 마을·일차가 유효하지 않으면 첫 구간으로 대체
- [x] 실제 새로고침으로 복원 확인

### 엑셀 `전체` 시트와 CSV에 마을 열 추가
- [x] `COLS`·`WID`·`dataRow`에 마을 열 추가, 병합·필터 범위를 N → O로 확장
- [x] `makeCsv` 머리글과 행에 마을 추가
- [x] 엑셀 재불러오기는 머리글 이름으로 열을 찾으므로 영향 없음을 확인

### 화면 확대 허용
- [x] viewport 메타에서 `maximum-scale=1` 제거

### 사진 보관함 (IndexedDB)
- [x] IndexedDB 저장소 구성 (`photos` · `parcel`/`at` 인덱스)
- [x] 촬영 시 축소본(장변 900px) 생성해 원본과 함께 보관
- [x] 원본은 최근 100장만 유지하고 오래된 것부터 자동으로 비움 (105장 넣어 100장 유지 확인)
- [x] 카드의 `사진 N장` 배지를 버튼으로 바꿔 누르면 사진 목록 표시
- [x] 사진별 `다시 저장`(원본이 남아 있을 때)·`삭제` 동작
- [x] 삭제 시 기록 매수도 함께 줄어드는지 확인
- [x] 설정에 보관 현황·전부 비우기 추가
- [x] 보관 기능 이전에 찍은 필지에서 안내 문구가 나오는지 확인
- [x] 터치 영역 확보 (사진 버튼 44px · 배지 30px)

### 마무리
- [x] 두 파일 JS 문법 검사 통과
- [x] 검증 스크립트 62개 항목 전체 통과
- [x] 실제 브라우저에서 촬영·취소·조회·삭제·비우기 동작 확인, 콘솔 오류 없음
- [x] 커밋 · 푸시

---

## 3차 · 스와이프와 사진 저장 흐름 (완료)

### 좌우 스와이프로 마을 넘기기
- [x] 카드 목록 영역에만 제스처를 걸어 상단 탭 줄의 가로 스크롤과 충돌하지 않게 함
- [x] 상단 칩 순서를 그대로 따라감 (마을 4개 → 완료)
- [x] 탭과 스와이프가 `goChip()` 하나를 함께 쓰도록 일원화
- [x] 세로 스크롤·짧은 이동(64px 미만)·화면 좌우 끝 24px 시작은 무시
- [x] 양 끝에서는 되감기 없이 안내만 표시
- [x] 방향에 맞춘 미끄러짐 애니메이션, `prefers-reduced-motion` 존중
- [x] 인정 시간 1200ms — 700ms 는 천천히 미는 경우 놓쳤다 (테스트 중 988ms 사례 확인)
- [x] 실제 터치 이벤트로 검증

### 촬영 즉시 갤러리 저장 (안드로이드 기준)
- [x] `IS_IOS` 판정과 `exportPhoto()` 도입
- [x] 안드로이드·PC — 촬영하면 공유창 없이 파일명 그대로 바로 저장
- [x] 아이폰 — 앱에 보관만 하고, 사진 목록의 `사진첩에 저장`으로 공유
- [x] 사진 목록 버튼 이름을 `다시 저장` → `사진첩에 저장`으로 변경
- [x] 실제 브라우저에서 다운로드 발생·파일명 유지·공유창 미호출 확인

### 마무리
- [x] 검증 스크립트 확장 후 전체 통과
- [x] 커밋 · 푸시
- [ ] **아이폰 사용자 1명 실제 확인 필요** — 촬영 시 공유창이 안 뜨는지, 사진 목록의 `사진첩에 저장`으로 사진첩에 들어가는지
- [ ] **안드로이드 실제 확인 필요** — 크롬이 처음 한 번 "여러 파일 다운로드 허용" 을 물을 수 있다. 허용해야 이후가 조용하다

---

## 4차 · 사진 매수 바로잡기 (완료)

- [x] 사진 목록에 `사진 기록 초기화` 버튼 추가 — 매수를 0장으로 되돌리고 앱 보관본도 함께 지운다
- [x] 확인 표시·확인 시각·메모는 건드리지 않는다 (실제로 보존되는지 검증)
- [x] 기록 매수와 앱 보관 장수가 다르면 시트 부제에 둘 다 표시 (`기록 3장 · 앱 보관 0장`)
- [x] 초기화 후 배지가 사라지고 시트가 닫히는지 확인
- [x] 검증 스크립트 통과 · 실제 브라우저에서 두 가지 상황 모두 확인
- [x] 커밋 · 푸시

---

## 5차 · 통신 없는 곳에서 위치 확인 (완료)

### 필지 좌표 확보
- [x] `_source` 접근성분석 엑셀에서 경도·위도 추출 (`tools/build_geo.py`)
- [x] 동부리는 열 구성이 달라 전체주소에서 마을·지번을 뽑아내 합침
- [x] **3,054필지 중 3,045건(99.7%)** 에 좌표 부여
- [x] 매칭 검증 — 엑셀의 `도로이격`·`접근성`을 앱 값과 2,939건 전수 대조, **불일치 0건**
- [x] 마을별 좌표가 반경 1.1~2.3km 안에 뭉쳐 있는지 확인
- [x] 좌표가 비어 있는 9필지는 키를 만들지 않고 화면에서 안내

### 펼쳐지는 약도
- [x] 지번을 누르면 카드 안에서 펼쳐지고 다시 누르면 닫힘
- [x] 대상 필지를 한가운데 두고 주변 필지·같은 날 필지(순번 표시)를 함께 그림
- [x] 그날 도는 순서를 점선으로 연결
- [x] 축척 막대와 방위표, 200m·400m·1km 세 단계 확대
- [x] 목록을 다시 그려도 펼쳐둔 약도가 유지됨

### GPS 현재 위치
- [x] `watchPosition` 정밀 모드로 현재 위치를 점으로 표시 (정확도 원 포함)
- [x] 필지까지 거리와 방향을 글로 안내 (`여기서 104m 북서쪽 · 오차 6m`)
- [x] 화면 밖이면 가장자리에 방향 화살표
- [x] 네 방향 전수 검증 — 북쪽에 서면 필지는 남쪽 199m
- [x] 위치값이 숫자가 아닐 때 지도가 깨지지 않도록 방어

### 마무리
- [x] 지도 타일을 한 장도 받지 않음 — 완전 오프라인
- [x] 용량 kim +46KB / moon +44KB
- [x] 검증 스크립트 전체 통과 · 실제 브라우저에서 콘솔 오류 없음
- [x] 커밋 · 푸시
- [ ] **현장 확인 필요** — 통신 음영지역에서 GPS 가 실제로 몇 초 만에 잡히는지

---

## 6차 · 연속지적도로 다시 그리기 (완료)

점만 찍힌 약도로는 위치 감을 잡을 수 없다는 현장 의견에서 시작했다.

### 지적도 도형 심기
- [x] 사용자가 브이월드에서 받은 연속지적도 shapefile 확인 (BUBUN 15,517필지, 좌표계 이미 WGS84)
- [x] `tools/build_cadastral.py` — 담당 리만 골라 단순화·부호화해 심는다
- [x] 더글러스-포이커 0.4m 단순화 · 폴리라인 부호화 (좌표점 21.9만 → 9만)
- [x] 조사원별로 담당 4개 리만 넣어 용량 절반 (kim 8,020필지 / moon 7,282필지)
- [x] **3,054필지 중 3,049건(99.8%)** 이 경계와 연결됨
- [x] **면적 교차검증** — 그린 경계에서 면적을 되짚어 대장과 대조, 중앙값 오차 0.7%
- [x] 파일당 0.44MB 증가에 그침

### 화면
- [x] 필지를 실제 경계 모양으로 그림 (대상 필지는 붉은 칸)
- [x] 도로를 흰 칸으로 그려 농로망이 드러나게 함 — 방향 감각의 핵심
- [x] 지목별 색 구분 (답·전·도로·물길·대지·임야)
- [x] 우리 필지는 초록 테두리, 그날 도는 필지는 더 굵게
- [x] 나침반 — 보는 방향을 위로 돌리되 글자는 뒤집히지 않게 좌표 계산에 회전을 넣음
- [x] 성능 — 지적도 푸는 데 12ms, 화면 한 장 5~6ms

### 마무리
- [x] 검증 138개 항목 전체 통과
- [x] 사용자 미리보기 확인
- [x] 커밋 · 푸시
- [ ] 지적도에 없는 5필지 — `양천리 329-2·330`, `황계리 544-1`, `신룡리 250-8·252-10`.
      연속지적도 자체에 지번이 없다. 조준 표시만 뜨고 경계는 안 나온다.

---

## 보류 · 사용자 확인 대기

- [x] ~~**네이버 Keep 사진 저장**~~ — 확인 완료. 공유창으로 킵에 저장은 되지만 파일명이 `share7081604304662116860` 형태로 바뀐다. 사진 앱도 마찬가지. 받는 앱의 동작이라 코드로 고칠 수 없다. **지금 방식을 유지하기로 결정** (2026-08-13). 필지 구분은 앱의 사진 보관함에서 한다.
- [ ] 사진에 이름표 새기기 (A안) — 보류. 필요해지면 `context-notes.md` 참고.
- [ ] 하루치 사진 zip 내보내기 (B안) — 보류. 기존 `zipStore()` 재사용 가능.
- [ ] 사진첩의 기존 사진을 필지에 붙이는 기능 — 보관 기능 이전 사진은 앱에 없다. 요청 시 검토.
- [ ] 전체 검색 (마을·일차 무시하고 1,500여 필지 전체에서 지번 검색)
- [ ] 오늘 일정 바로가기 (44일 탭 중 오늘 구간으로 이동)
- [ ] 네이버지도 앱 미설치 시 아이폰 폴백이 팝업 차단되는지 — 앱 없는 폰에서 확인 필요
- [ ] 깃허브 자동 동기화 (조사원 폰에 PAT 1회 입력)
- [ ] 구글 드라이브 사진 자동 업로드
