# 작업 노트

작업 중 내린 결정과 그 이유를 계속 덧붙이는 문서. 다음 세션이 배경을 다시 캐지 않아도 되도록 남긴다.

---

## 앱 구조 (2026-08-13 파악 · 2026-09-07 갱신)

- `kim.html` — 김정원 조사원용. 1,554필지. 광천리·덕촌리·서부리·양천리. 1~44일차.
- `moon.html` — 문현정 조사원용. 1,500필지. 동부리·황계리·남전리·신룡리. 1~43일차.
- 두 파일은 **`PARCELS` 배열 · `KEY` 저장키 · `<title>` · 헤더 부제 · 엑셀 파일명**만 다르고 나머지 코드는 동일하다.
- `lee.html` — 이승현 주무관용. **다른 사업이다.** 2026년 논 타작물 재배지원 신청농지 85필지의 이행점검.
  앞의 두 파일에서 지도 엔진·사진 보관함·XLSX 생성기/판독기를 그대로 잘라 썼지만 데이터 모델과 화면은 다르다.
  자세한 것은 아래 [논 타작물 이행점검 앱](#논-타작물-이행점검-앱-leehtml--2026-09-07) 절에 있다.
- 외부 라이브러리 없음. XLSX 생성기와 파서를 직접 구현해 넣었다 (ZIP store 방식 + `DecompressionStream`).
- GitHub Pages로 배포되어 조사원들이 카카오톡 링크로 접속한다. 현재 현장에서 사용 중.

## 결정 사항

### 파일을 합치지 않고 두 개를 그대로 유지한다 (2026-08-13)

공통 로직을 `app.js`로 빼면 유지보수가 편해지지만, 현장 조사가 44일째 진행 중이라 배포 구조를 바꾸는 위험이 이득보다 크다. 사용자도 "지금 구조 유지"를 선택했다. 대신 **모든 수정은 두 파일에 반드시 똑같이 적용한다**는 규칙으로 대체한다.

### `동부리` 하드코딩은 상수 두 개로 대체한다 (2026-08-13)

`moon.html`을 복사해 `kim.html`을 만들면서 남은 자국이다. 보고서 제목, 엑셀 시트 제목, CSV·백업 파일명 총 8곳에 `동부리`가 박혀 있었다.

마을명을 매번 나열하는 대신, 스크립트 상단에 `WHO`(조사원명)와 `AREA`(행정구역)를 두고 마을 목록은 이미 있는 `VILLAGES`에서 뽑아 쓴다. 앞으로 조사원이나 담당 구역이 바뀌어도 상단 두 줄만 고치면 된다.

**파일명에는 마을명이 아니라 조사원명을 쓴다.** 담당 마을이 4개라 `광천리·덕촌리·서부리·양천리_조사기록.csv`처럼 되면 너무 길고, 엑셀 파일명이 이미 `김정원_조사기록.xlsx`라 그쪽에 맞추는 게 일관적이다.

### 보고서 팝업 차단은 blob 링크로 우회한다 (2026-08-13)

기존 코드는 `window.open`이 막히면 토스트만 띄우고 끝났다. 아이폰에서 실제로 자주 막힌다.

`document.write` 방식을 1순위로 남겨둔 이유는, iOS 사파리에서 `blob:` URL을 새 탭으로 여는 게 버전에 따라 불안정한 이력이 있어서다. 팝업이 열리면 지금까지 잘 되던 경로를 그대로 쓰고, **막혔을 때만** blob 링크를 시트에 띄운다. 사용자가 직접 누르는 앵커 클릭은 팝업 차단 대상이 아니라 확실히 열린다.

### 좌표는 사무실 접근성분석 엑셀에서 나왔다 (2026-08-21)

앱의 `도로에서 42.6m`, `인접 도로 1305-16 도` 같은 값은 지적도 도형이 있어야만 계산된다. 그 계산을 한 파일이 곧 좌표의 출처였고, 실제로 `_source/개령면_접근성분석결과.xlsx`에 **경도·위도 열이 그대로 들어 있었다.**

매칭이 맞는지는 지번 대조만으로 판단하지 않았다. 엑셀에도 `도로이격(m)`과 `접근성`이 있어서 앱의 값과 2,939건을 전부 대조했고 **불일치가 한 건도 없었다.** 이 엑셀이 앱 데이터의 원본이 맞다는 뜻이라, 좌표를 그대로 신뢰했다.

동부리는 처음 분석에서 통째로 빠져 있어 나중에 따로 받았다(`동부리_접근성분석결과_v2.xlsx`). 이 파일은 열 구성이 달라 `village`/`jibun` 대신 `전체주소`에서 뽑아 쓴다. `tools/build_geo.py`가 두 파일을 함께 읽어 합친다.

최종 99.7%. 남은 9필지는 원본 엑셀에서 좌표 칸 자체가 비어 있던 것이라 우리 쪽에서 채울 방법이 없다.

### 지도를 이미지가 아니라 벡터로 그린 이유 (2026-08-21)

지도 타일을 미리 받아 앱에 넣는 방식은 네이버·카카오·VWorld 모두 약관에서 금지한다. VWorld 지오코더는 "별도의 저장장치나 데이터베이스에 저장할 수 없다"고 명시돼 있어 좌표를 그쪽에서 받아 심는 것도 안 된다.

그래서 좌표만 앱에 넣고 **화면은 앱이 직접 그린다.** 타일을 한 장도 받지 않으므로 통신이 완전히 끊겨도 동작하고, 용량도 파일당 45KB 안팎이다.

**GPS 는 통신과 무관하다.** 위성 신호라 데이터가 끊겨도 위치가 잡힌다. 음영지역에서 안 되는 것은 지도 이미지를 받아오는 일이지 측위가 아니다. 다만 보조 정보를 못 받아 첫 측위가 느릴 수 있다.

좌표 변환은 평면으로 근사했다. 면 하나 크기(약 7×7km)에서는 오차가 무시할 만하고, 네 방향 200m 지점을 넣어 검증했을 때 199~200m 로 나왔다.

### 연속지적도를 어떻게 1MB 안에 넣었나 (2026-08-21)

점만 찍힌 약도로는 밭에서 위치 감을 못 잡는다는 현장 의견을 받고 진짜 경계로 바꿨다. 사용자가 브이월드에서 김천시 연속지적도를 받아 `_source` 에 넣어 주었다(`LP_PA_CBND_BUBUN`, 15,517필지). 좌표계가 이미 WGS84 라 변환이 필요 없었다.

용량을 줄인 방법은 세 가지다.

- **담당 리만 넣는다.** 두 조사원이 서로 다른 4개 리를 맡으므로 각 파일에 절반씩만 들어간다.
- **0.4m 단순화.** 화면에서 구분되지 않는 꼭짓점을 더글러스-포이커로 걸러 좌표점이 21.9만에서 9만으로 줄었다.
- **폴리라인 부호화.** 구글 방식 그대로, 앞 점과의 차이를 5비트씩 끊어 아스키로 감는다. 정밀도는 1e-6(약 0.11m)로 남겼다.

결과는 파일당 0.44MB. 푸는 데 12ms, 화면 한 장 그리는 데 5~6ms 다.

매칭은 `(리, 본번, 부번)` 으로 맞췄다. 지적도의 `bubun` 필드에 지목 글자가 붙어 있어(`7도`) 숫자만 뽑아 쓴다. 검증은 지번 대조로 끝내지 않고 **그린 경계에서 면적을 되짚어 대장 면적과 대조**했다 — 중앙값 오차 0.7%.

**도로를 흰 칸으로 칠한 것이 핵심이다.** 지적도에는 농로 1,791필지, 구거 432, 하천 121 이 모두 필지로 들어 있어서, 흰색으로 두면 밭 사이 길이 여백처럼 드러난다. 실제 지적도의 관행이기도 하고 방향 잡기에 가장 도움이 된다.

지적도에 아예 없는 지번이 5개 있다. 최근 분할·합병됐거나 갱신이 안 된 것으로 보인다. 그 필지는 경계 없이 조준 표시만 뜬다.

### 나침반 회전은 좌표 계산에 넣었다 (2026-08-21)

SVG `transform="rotate()"` 로 통째로 돌리면 지번 글자까지 뒤집힌다. 그래서 회전을 투영 함수 안에 넣어 점 위치만 돌리고 글자는 똑바로 그린다.

방위는 아이폰이 `webkitCompassHeading`, 안드로이드가 `deviceorientationabsolute` 의 `alpha` 로 준다. 아이폰은 13 이상에서 사용자 동작에 이어 권한을 물어야 해서 버튼 눌렀을 때 요청한다. 3도 이상 움직였을 때만 다시 그려 불필요한 갱신을 줄였다.

### 약도에 동선 점선을 넣은 이유 (2026-08-21)

400m 범위에 필지가 10개 남짓이라 점만 찍으면 화면이 휑하고 방향을 잡기 어려웠다. 밭 한가운데는 지형지물도 없다. 그날 도는 순서(`seq`)를 점선으로 이으니 "내가 몇 번에 있고 다음이 어느 쪽인지"가 한눈에 보인다. 이미 가지고 있는 데이터로 만든 것이라 추가 자료가 필요 없다.

### 사진 매수와 앱 보관본은 일부러 따로 논다 (2026-08-14)

`shots`는 **조사 기록**이다. 엑셀·CSV·보고서에 그대로 들어간다. 앱 보관본은 **확인용 사본**일 뿐이다. 그래서 설정의 `전부 비우기`는 보관본만 지우고 매수는 건드리지 않는다 — 사진첩에 있는 진짜 사진은 그대로 있으니 매수를 지우면 오히려 조사 기록이 틀려진다.

문제는 이 둘이 어긋났을 때 화면에 아무 표시가 없어서, 사용자가 "사진을 지웠는데 왜 3장이라고 나오지"라고 느낀다는 것이었다. 그래서 두 가지를 넣었다.

- 어긋나면 시트 부제에 `기록 3장 · 앱 보관 0장`처럼 **둘 다 보여준다.** 같으면 `기록 3장`만 나온다.
- `사진 기록 초기화` 버튼으로 그 필지만 0장으로 되돌린다. 보관본도 함께 지워 둘이 다시 맞는다.

초기화는 `shots`와 `pn`만 0으로 만든다. **확인 표시·확인 시각·메모는 손대지 않는다** — 사진을 다시 찍는 것과 조사를 취소하는 것은 다른 일이다.

전체 필지를 한꺼번에 초기화하는 기능은 만들지 않았다. 매수는 조사 기록이라 일괄로 날리는 건 위험하고, 요청받은 것도 필지 단위였다.

### 조사원은 안드로이드가 다수, 아이폰이 한 명이다 (2026-08-13)

이 사실이 사진 저장 설계를 뒤집었다. 그전까지는 아이폰 기준으로 맞추느라 모두가 공유창을 거쳐야 했다.

- **안드로이드·PC** — 앵커 다운로드(`dl()`)가 잘 동작하고, **파일명이 그대로 유지된 채** 다운로드 폴더에 들어가 갤러리에서 보인다. 그래서 촬영하면 묻지 않고 바로 저장한다.
- **아이폰** — 같은 방식이 사진첩이 아니라 파일 앱으로 가버린다. 그래서 촬영 시에는 앱에만 보관하고, 사진 목록의 `사진첩에 저장` 버튼으로 공유창을 띄운다.

판정은 `IS_IOS`로 한다. 기능 감지가 아니라 userAgent 기반인데, "다운로드가 사진첩으로 가는가"는 감지할 수 있는 성질이 아니기 때문이다. iPadOS 13+ 는 자신을 Mac 으로 보고하므로 `maxTouchPoints`를 함께 본다.

이 변경으로 **안드로이드 쪽은 파일명 문제 자체가 사라진다.** 파일명이 망가지는 건 이제 아이폰 한 명뿐이다.

### 촬영 취소 분기는 없어졌다 (2026-08-13)

`shoot()`에서 `saveOrShowPhoto()`의 반환값을 보고 매수를 올릴지 정하던 분기를 뺐다. 촬영 시점에 공유창을 띄우지 않게 되어 취소할 일 자체가 없어졌기 때문이다. 안드로이드는 바로 저장되고, 아이폰은 앱에 보관만 한다. 아이폰에서 나중에 `사진첩에 저장`을 눌렀다가 취소해도 이미 찍은 사실은 변하지 않으므로 매수를 건드리지 않는 게 맞다.

### 공유한 사진의 파일명은 지키지 않기로 했다 (2026-08-13)

앱은 `new File([file], name)`으로 정확한 이름을 붙여 `navigator.share()`에 넘긴다. 그런데 **받는 앱이 그 이름을 버린다.** 실제로 확인된 것은 두 곳이다.

- 아이폰 사진 앱 — 아이폰 자체 이름으로 바뀜
- 네이버 킵 — `share7081604304662116860` 형태로 바뀜

공유 시트로 넘긴 뒤에는 웹페이지가 관여할 수 없는 영역이라 코드로 고칠 방법이 없다.

검토한 대안은 두 가지였다. **(A) 사진에 이름표를 새겨 넣기** — 파일명이 바뀌어도 사진만 보면 필지를 알 수 있지만, 재인코딩 때문에 EXIF가 사라진다. **(B) 하루치를 zip으로 묶어 내보내기** — zip 안의 파일명은 바깥 앱이 못 건드리므로 이름이 살아남는다. 기존 `zipStore()`를 재사용하면 된다.

사용자는 **둘 다 하지 않고 지금 방식을 유지**하기로 했다. 사진첩에는 이름이 바뀐 채로 저장되고, 어느 필지 사진인지는 앱의 사진 보관함에서 확인한다. 앱 안에서는 원래 파일명이 그대로 보인다.

나중에 필요해지면 A와 B 모두 이 노트만 보고 바로 착수할 수 있다.

### 사진 보관함은 소급되지 않는다 (2026-08-13)

보관 기능이 생기기 전에 촬영한 사진은 앱에 사본이 없어서 목록에 뜨지 않는다. 해당 필지를 열면 안내 문구가 나온다. 이미 찍은 사진을 앱에 붙이려면 사진첩에서 직접 골라 넣는 기능이 필요한데, 파일명이 이미 망가진 상태라 어느 필지 것인지 사람이 판단해야 한다. 요청받지 않았으므로 만들지 않았다.

### 네이버 Keep 자동 업로드는 불가능하다 (2026-08-13)

네이버 개발자센터와 네이버 클라우드 연동 API 어디에도 Keep·MYBOX에 파일을 올리는 공개 API가 없다. 구글 드라이브처럼 "자동 업로드" 선택지 자체가 존재하지 않는다.

다만 사진 촬영 시 이미 `navigator.share({files})`로 iOS 표준 공유창을 띄우고 있으므로, 공유창에 네이버 앱이 나타나면 **코드 수정 없이** Keep으로 수동 저장이 가능하다. 공유창에 실제로 뜨는지와 파일명 보존 여부는 조사원 폰에서 확인해야 한다.

### 보던 위치는 진행상황과 분리해 저장한다 (2026-08-13)

`localStorage`의 `KEY+':pos'`에 따로 넣는다. 백업 JSON에 섞으면 다른 폰에서 백업을 복원했을 때 그 폰의 화면 위치까지 덮어써진다. 진행상황(확인·사진·메모)은 기기 사이를 옮겨 다녀야 하지만 화면 위치는 그 기기의 것이다.

### 사진은 축소본과 원본을 다르게 다룬다 (2026-08-13)

용량 계산이 설계를 결정했다. 하루 35필지에 필지당 1~2장이면 약 50장, 아이폰 원본이 장당 3MB 안팎이라 44일이면 6.6GB다. 브라우저 저장소가 감당하지 못하고 중간에 저장이 실패한다.

그래서 두 가지로 나눴다.

- **축소본**(장변 900px, JPEG 품질 0.7) — 전 기간 보관. 무엇을 찍었는지 확인하는 용도. 장당 0.1~0.3MB.
- **원본** — 최근 100장만 유지. 공유창에서 저장을 놓쳤을 때 다시 내보내기 위한 것. `at` 인덱스를 역순 커서로 훑어 오래된 것부터 `full`만 `null`로 만든다. 축소본은 남으므로 사진 목록에서 계속 보인다.

`getAll()`로 전체를 읽지 않고 커서를 쓰는 이유는, 수천 장이 쌓였을 때 레코드 객체를 한꺼번에 만들지 않기 위해서다.

**앱 보관본은 백업이 아니다.** 사파리는 오래 쓰지 않은 사이트의 저장 데이터를 지운다. 홈 화면에 추가해 매일 쓰면 유지되지만 보장은 아니다. 사진 목록과 설정 화면 양쪽에 "최종 사진은 사진 앱이나 드라이브에 저장하라"고 적어뒀다.

### 사진 번호는 `pn`으로 따로 센다 (2026-08-13)

`shots`는 현재 매수라서 삭제하면 줄어든다. 파일명 번호를 `shots+1`로 매기면 삭제 후 새로 찍었을 때 이미 있는 키와 부딪힌다. 그래서 "지금까지 쓴 가장 큰 번호"를 `pn`에 따로 두고 `pn+1`을 쓴다. 번호가 중간에 건너뛸 수 있지만 충돌은 없다.

### 사진 저장 취소 판정은 AbortError 하나뿐이다 (2026-08-13)

`saveOrShowPhoto()`가 `false`를 돌려주는 건 사용자가 공유창을 취소했을 때(`AbortError`)뿐이다. 공유가 아예 안 되는 환경에서 이미지를 띄우고 "길게 눌러 저장하세요"로 넘어가는 경로는 실제 저장 여부를 알 방법이 없어 `true`로 둔다. 이 경우 매수는 올라간다 — 알 수 없는 것을 아는 척하지 않는 쪽을 택했다.

## 논 타작물 이행점검 앱 (lee.html · 2026-09-07)

전수조사와는 다른 사업이다. **2026년 논 타작물 재배지원**에 신청한 농지를 현장에서 확인하고
대장의 `이행면적`·`점검자`·`1차점검` 칸을 채우는 일이며, 담당은 이승현 주무관이다.
원본은 사용자가 준 `통합 문서1.xlsx` 의 `김천 (2)` 시트(1,197행, 8.28. 기준).

### 세 번째 파일로 나눈 이유

kim/moon 처럼 상수 몇 줄로 갈라놓을 수 없다. 기록하는 것이 다르기 때문이다.
전수조사는 필지를 **봤는지 안 봤는지**(`done` 참/거짓)를 남기지만, 이행점검은
**적합·부적합·보류 판정과 이행면적(㎡)** 을 남긴다. 카드에 띄우는 항목도 지목·소유자·접근성이 아니라
신청품목·신청인·대분류〉중분류·전년품목이다. `PARCELS` 의 열 자체가 겹치지 않는다.

**대신 세 파일이 로직을 공유하게 됐다.** `lee.html` 의 지도 엔진(`miniSvg`~`stopGps`),
사진 보관함(IndexedDB), XLSX 생성기·판독기는 `kim.html` 에서 그대로 잘라 온 것이다.
이 부분을 고칠 일이 생기면 **세 파일 모두에 똑같이 적용해야 한다.**

### 대상은 '읍면동' 칸이 아니라 '농지 소재지'로 골랐다

대장에서 `읍면동` 칸이 개령면인 행은 101건, 농지 소재지가 개령면인 행은 85건이고,
둘이 어긋나는 것이 22건이다. 신청인이 다른 면에 살거나 그 반대인 경우다.
이행점검은 **밭에 가는 일**이라 농지가 어디 있느냐로 잘랐다.

결과는 85필지 · 신청인 19명 · 광천리 60 · 양천리 17 · 서부리 4 · 덕촌리 2 · 남전리 1 · 동부리 1.
신청면적 합계 175,424.66㎡.

참고로 대장 6행의 `계`(공부상 175,728.66 · 신청 175,424.66)는 전체 1,197행의 합이 아니라
**개령면 85건의 합과 정확히 일치한다.** 예전에 걸러 보던 상태로 남은 수식으로 보인다.
전체 1,197행의 신청면적 합은 2,197,555.52㎡ 다.

### 좌표를 새로 만들지 않고 전수조사 앱에서 꺼내 왔다

브이월드 지오코더는 결과를 저장하지 못하게 약관으로 막고 있고, 연속지적도 원본
(`_source/`)은 이 저장소에 없다. 그런데 개령면 8개 리의 좌표와 지적도 경계가
**이미 `kim.html`·`moon.html` 안에** 들어 있었다. 그래서 `(리, 지번)` 으로 맞춰 가져왔다.

**85필지 중 40필지가 맞았다.** 나머지 45필지는 지번 형식 문제가 아니라
2025년 전수조사의 대상 목록(2,943필지) 자체에 없던 지번이다 —
`개령면_필지목록.csv` 로 대조해 확인했다. 그 조사는 답·전 위주로 추려진 것이라
논 타작물 신청농지와 완전히 겹치지 않는다.

좌표가 없는 45필지는 약도 자리에 사유를 적고 네이버지도 버튼으로 넘긴다.
나중에 연속지적도를 다시 받으면 `tools/build_nongji.py` 로 채울 수 있다.

### 지적도는 신청농지 반경 700m 만 남겼다

두 파일의 지적도를 합치면 15,302 폴리곤이지만, 좌표가 붙은 필지가 40개뿐이라
전면을 다 넣을 이유가 없다. 약도의 최대 배율이 1km(반폭 780m)이므로 700m 로 잘라
**8,167 폴리곤 · 381KB** 가 됐다. 파일 전체는 537KB 로 원본 두 파일(약 1MB)보다 작다.

### 순번은 이동경로다 (2026-09-07 · 처음 만들 때와 달라진 부분)

처음에는 리 → 신청인(가나다) → 지번(숫자) 순으로만 정렬했다. 전수조사와 달리
이 대장에는 사무실에서 짠 일정표가 없었고, 한 농가의 필지를 한 번에 도는 것이
자연스럽다고 봤기 때문이다. **둘 다 틀렸다.**

사용자가 "출장할 때 순서대로 가야 한다"고 해서 실제로 재보니,
좌표가 있는 40필지만 따져도 **그 순서로 돌면 17.4km, 경로를 짜면 6.5km** 였다.

**신청인으로 묶는 것 자체가 동선을 망친다.** 장찬우 님 필지는 최대 2,832m,
배언 님은 1,345m 흩어져 있어 농가별로 돌면 같은 자리를 두 번 지난다.
반대로 김경권·김지원 님은 25~28m 안에 다 모여 있다 — 묶어도 이득이 없다.
그래서 **신청인을 무시하고 순수 이동경로로** 순번을 다시 매겼다.

경로는 리 안에서만 짠다(리 사이는 사용자가 하루 단위로 정한다). 필지가 30개 안쪽이라
**모든 필지를 한 번씩 출발점으로 삼아** 최근접 이웃 + 2-opt 를 돌리고 가장 짧은 것을 쓴다.
한 리에 0.05초라 굳이 아낄 이유가 없다. 앞 리에서 넘어오는 쪽에 가까운 끝에서
시작하도록 경로의 방향만 뒤집는다.

**좌표 없는 45필지가 이 순서의 약점이다.** 32개는 같은 리에 있는 같은 신청인의
필지 뒤에 붙였다 — 한 농가의 밭은 대개 가까이 있다는 **추정**이지 확인된 위치가 아니다.
나머지 13개는 기댈 곳이 없어 리의 맨 뒤로 보냈다(양천리 서순자 5, 광천리 우창하 2,
양천리 김지원 2, 그 밖 4). 카드에 `위치 자료 없음`으로 표시해 순서를 곧이곧대로
믿지 않도록 했다. 연속지적도를 받아 좌표를 채우면 이 약점이 사라진다.

신청인 탭은 **거르개로 남겨 뒀다.** 기본은 `전체`(경로 순서)이고, 특정 농가만 볼 일이
있을 때 쓴다. 탭을 골라도 순서는 경로 그대로다. 검색창에 이름을 쳐도 같은 일이 된다.

약도의 동선 점선과 강조선도 신청인이 아니라 **그 리를 도는 순서**를 따른다.
카드에는 경로상 다음 필지까지의 거리(`다음 476m`)를 띄운다 — 미리 계산해
`PARCELS` 의 `dn` 에 넣어 두었다(마지막 필지와 좌표 없는 필지에는 없다).

결과: 재배열 전 17,435m → 6,454m (광천리 4,397 · 양천리 1,666 · 덕촌리 391).

> 2-opt 에서 한 번 크게 헤맸다. 꼬리가 있는지를 `if d`(다음 노드 번호)로 판단했더니
> **번호가 0인 노드를 '꼬리 없음'으로 읽어** 길이 계산이 어긋났고, 이득이 아닌 뒤집기를
> 무한히 반복했다. 출발점을 0번으로 잡은 시험에서는 0번이 늘 맨 앞이라 재현되지 않아
> 더 헷갈렸다. 자리(`k+1 < n`)로 판단하도록 고쳤다.

### 판정에 '보류'를 넣었다

대장의 `1차점검` 칸에는 적합·부적합만 들어가지만, 현장에서 판단이 안 서는 필지를
**아직 안 간 필지와 구분**할 방법이 필요했다. 보류로 찍으면 완료 탭으로 넘어가
목록에서 사라지되, 요약과 엑셀에서 따로 세어진다. 대장에 옮길 때는 사람이 정리한다.

### 이행면적을 비워 두면 신청면적을 쓴다

적합이 대부분일 것이고, 그때 이행면적은 신청면적과 같다.
매번 같은 숫자를 치게 하지 않으려고 빈칸을 신청면적으로 읽는다.
입력칸 안내문에 그 숫자를 미리 띄워 둔다.

### 엑셀 열 순서는 관리대장 그대로다

`순번·읍면동·리·신청인·주소·연락처·농지 소재지·지번·공부상면적·신청면적·대분류·중분류·신청품목·
대구분·구분·전년품목·이행면적·점검자·1차점검·사진·점검시각·비고(특이사항)·현장 메모·지도`.
대장에 그대로 옮겨 붙이려는 것이라 앱이 채우는 칸(노란색)만 뒤쪽에 몰아 두지 않고
**대장에 있는 자리 그대로** 뒀다. 되불러오기는 `전체` 시트에서 `지번`+`신청인`+`리` 로 맞춘다
— 같은 지번이 리마다 있을 수 있어서다.

### 검색은 전체를 훑는다

kim/moon 에서 보류로 남겨 둔 항목(`checklist.md` 의 "전체 검색")을 여기서는 처음부터 그렇게 만들었다.
85필지뿐이라 전량을 훑어도 부담이 없고, 현장에서는 "이 지번이 신청 목록에 있나"를
확인하는 쓰임이 더 많다. 지번·신청인·품목·중분류·연락처·농지 소재지를 함께 본다.

### 다시 만드는 법

대장이 갱신되면 `tools/build_nongji.py` 로 `lee.html` 의 데이터 블록만 갈아 끼운다.
앱의 나머지 코드는 건드리지 않는다.

```
python tools/build_nongji.py            # ../gaeryeong-survey 를 기본으로 본다
python tools/build_nongji.py <저장소경로>
```

`통합 문서1.xlsx` 는 저장소에 없다(개인정보가 들어 있다). 사용자 PC의
`D:\user\Desktop\농지전수조사\` 에 앱과 나란히 둔다. 스크립트는 그 위치를 기준으로 찾는다.
다시 돌려도 안전하다 — 실제로 재실행해 결과가 같은 것을 확인했다.

## 손대지 않은 것 (인지하고 있으나 요청 범위 밖)

- `importXlsx()`의 `const need=['지번','확인','사진','현장']` — 선언만 되고 쓰이지 않는 죽은 변수.
- `tableSheet(name, list, tint, subtitle)`의 `tint` 인자 — 전달만 되고 함수 안에서 쓰이지 않는다.
- 검색이 현재 선택된 마을·일차 안에서만 동작하는 문제 — 별도 항목으로 `checklist.md` 보류 목록에 있다.
- ~~`PARCELS`에 위경도가 없다~~ — 해결됨. 2026-08-21 에 `GEO` 로 좌표를 심었다. "가까운 필지 순 정렬"도 이제 만들 수 있다.

`lee.html` 쪽:

- **2차점검은 앱에서 다루지 않는다.** 대장에는 `1차점검`·`2차점검` 두 칸이 있는데 앱은 1차만 기록한다.
  2차는 시기도 기준도 다른 별도 점검이라 요청 범위 밖으로 뒀다. 필요해지면 `state` 에 칸을 하나 더 두고
  판정 시트에 차수 전환을 붙이면 된다.
- **좌표가 없는 45필지.** 연속지적도를 다시 받으면 `tools/build_nongji.py` 로 채운다.
- `tableSheet()` 의 `tint` 인자는 여기서 아예 뺐다. kim/moon 에는 아직 남아 있다.
